node_modules/
.pnpm-store/

# Generated Obsidian plugin artifacts. Release artifacts are produced by the build.
main.js
pdf.worker.min.mjs
data.json

# Local editor, agent, test, and diagnostic state.
.obsidian/
.agents/
.cache/
.codex/
diagnostics/
coverage/
tests/dist/
release-staging/

# Internal handoff + research docs — for AI agents / future maintainers,
# NOT for end users. Public repo users only need README + CHANGELOG.
# These files still exist locally for hand-off, but tracked history stops here.
AGENTS.md
PDF-PLUS-ANALYSIS.md
reports/

# Never commit credentials, private keys, or machine-specific secrets.
.env
.env.*
!.env.example
*.pem
*.key

# Operating-system metadata.
.DS_Store
Thumbs.db
